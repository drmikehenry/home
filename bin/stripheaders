#!/usr/bin/env python3

import sys
import shutil

with open(sys.argv[1], "rb") as f:
    for line in f:
        if not line.rstrip(b"\n"):
            break
    shutil.copyfileobj(f, sys.stdout.buffer)
